arr = [1, 2, 3, 4, 5]
arr2 = ["dog", "cat", "bird", "snake"]

# ----------viewing and removing-----------

# arr.fetch(100, "oops")

# arr.first
# arr.last

# p arr.take(2)
# p arr.drop(2)

# p arr2.include?("cat")

# ----------add to an array-----------

# arr.push(6)
# arr << 6
# p arr.unshift(0)
# p arr.insert(4, "wildebeast")
# this is posit^ion

# -----------remove elements-------------

# arr.pop takes the last element off
# arr.shift retrieve and at the same time remove the first item
# arr.delete_at(2) deletes at the index
# arr.delete(2) removes all of what the input is